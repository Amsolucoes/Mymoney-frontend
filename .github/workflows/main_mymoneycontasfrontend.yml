name: Deploy to Azure

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    # Checkout do código
    - name: Checkout code
      uses: actions/checkout@v2
    
    # Instalação das dependências
    - name: 'Install dependencies'
      run: |
        npm install

    # Build do projeto
    - name: 'Build project'
      run: |
        npm run build

    # Deploy para o Azure usando o publish profile
    - name: 'Deploy to Azure'
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'mymoneycontasfrontend'  # Substitua pelo nome correto do seu App Service no Azure
        slot-name: 'Production'  # Ou o nome do seu slot específico
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_EE14B8B24ABF4BA7946516F1E5ACD346 }}  # Use o secret com o conteúdo do publish profile
